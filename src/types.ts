import {
  LogLevelEnum,
  LogChannelEnum,
  LogTagEnum,
} from '@/enums';

export type LogLevel = `${LogLevelEnum}`;

export type LogChannel = `${LogChannelEnum}`;

export type LogTag = `${LogTagEnum}`;
